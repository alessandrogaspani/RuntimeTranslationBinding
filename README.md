# RuntimeTranslationBinding
RuntimeTranslationBinding is a .NET 8 sample project that demonstrates how to implement runtime localization and culture switching in WPF and WinUI 3, allowing UI strings to update dynamically without restarting the application.
